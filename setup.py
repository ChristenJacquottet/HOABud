# aimakerspace/setup.py
from setuptools import setup, find_packages

setup(
    name="aimakerspace",
    version="0.1.0",
    packages=find_packages(),
    # leave this list empty if you prefer to manage deps in api/requirements.txt
    install_requires=[
        # add dependancies if needed here
    ],
)
